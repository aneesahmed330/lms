export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum UserRole {
  Admin = 'admin',
  Instructor = 'instructor',
  Student = 'student',
}

export const SupportedFileTypes = {
  IMAGE_TYPE: ['.png', '.jpeg', '.jpg'],
  DOC_TYPE: ['.doc', '.docx'],
};
